package build.driver;

import build.data.BaseShape;

public interface ShapeMovement {

    static void moveUp(BaseShape shape) {
    }

    static void moveDown(BaseShape shape) {
    }

    static void moveLeft(BaseShape shape) {
    }

    static void moveRight(BaseShape shape) {
    }

}
